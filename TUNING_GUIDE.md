# TUNING_GUIDE.md — 写给软件工程师的模型调优指南（用本项目 26 轮真实调参史教学）

> 读者假设：会写代码、会改配置、会用 git，但**没有机器学习/统计背景**。
> 全文只用到一个比喻体系：**训练一个模型 = 调一台会自己写答案的机器**；调参 = 给它换配置、跑基准测试、看日志。
> 本项目全部结论都能在这份指南里找到"人话版"；要看原始数据，读 `reports/tuning_rounds.csv`（每轮参数+指标+为什么调，一行一轮）。

---

## 1. 三分钟看懂这个项目在干什么

**任务**：家里只有一个"总电表"（每 6 秒记一次全家总功率），要猜出其中"烧水壶"这一件电器的功率曲线。

- 输入：过去 128 个采样点（约 12.8 分钟）的**总功率**序列；
- 输出：这 128 个点**正中间那一刻**烧水壶的功率（一个数字）；
- 模型：Transformer Encoder（可以理解成一个"会看上下文的滑窗回归器"）。

这种"不开传感器到电器上、只靠总表反推每台电器"的技术叫 **NILM（非侵入式负荷分解）**。

难点不在模型，在**数据分布**：烧水壶 99.37% 的时刻功率低于 500W（稠密评测 30000 个窗里只有 266 个算"在烧水"）。也就是说，这台机器要回答的问题 99% 都是"没有烧水"，1% 才是"在烧水、多少瓦"。**后面几乎所有调参故事的根源都是这 1%**——为了一分钟的真实信号，模型天天被海量"0"淹没。

## 2. 八个指标都是什么（人话 + 本项目实测基准）

评测集是 30000 个"窗"（每窗一个真值、一个预测值）。指标分两类。

### 2.1 回归类：猜数猜得准不准

| 指标 | 人话定义 | 类比 | 本项目参考值 |
|---|---|---|---|
| **MAE** | 平均每个窗猜错多少瓦 | 平均误差 | 稳定版 6.68W（起点 13.62W） |
| **RMSE** | 误差的"平方再开根"，大错罚得重 | P95 延迟 vs 平均延迟 | ~104W（被个别爆表事件主导） |
| **R²** | 比"永远猜均值"好多少（1=完美，0=没用） | 相对基线的提升率 | 0.77 |
| **SAE / energy_error** | 总电量猜偏了百分之几（带符号） | 账单总量误差 | SAE=|EE|=10.0%（EE=−10%，系统性低估） |

注意 SAE 在**本仓库**的定义是 `|总预测−总真值|/总真值`（等价于 |energy_error|，见 `src/metrics.py`），不是"逐点误差绝对值的均值"。读论文时留意同名不同义。

### 2.2 检测类：该报警时报警了吗（运维更关心这个）

把功率超过 **500W** 视为"在烧水"，每个窗就变成二分类：

- **Precision（查准率）**：模型喊"在烧水"的时刻里，多少真烧了。低=**误报多**。
- **Recall（查全率）**：真烧水的时刻里，模型抓住了多少。低=**漏报多**。
- **F1**：两者调和平均，`2PR/(P+R)`。它偏科就掉分：P=0.99,R=0.5 的 F1 只有 0.66。

本项目的结构性矛盾：**把报警门槛调低→少漏报但多误报（R↑P↓）**，两指标像跷跷板，调优的本质常常是"把跷跷板整体抬高"，而不是在板上挪位置。

### 2.3 训练/验证/测试三兄弟（铁律的来源）

数据按时间切成三份：train(70%) 用来学习；**val(15%) 用来看疗效**（选 epoch、调参数）；**test(15%) 是期末考试，只在一切定稿后考一次**。反复拿 test 挑配置 = 变相作弊（"考前偷看答案"），结论就不可信了。本仓库规则（REPORT.md §2）写死了这一点。

## 3. 配置文件里每个键是什么（`configs/baseline.yaml` → 每个 run 目录里的 `config.yaml`）

调参不是改代码，**90% 的动作只是改这几个配置值**：

| 键 | 人话 | 类比 |
|---|---|---|
| `d_model` | 模型的"脑容量"（内部向量宽度） | 给服务器加内存 |
| `num_layers` / `dim_feedforward` | 网络深度 / 每层处理宽度 | 加流水线工位 |
| `nhead` | 注意力"分头看"的份数 | 多路并发读 |
| `dropout` | 训练时随机屏蔽一部分信号，逼模型别死记硬背 | 强制不许背题 |
| `lr` | 学习率：每次改答案的步长 | 梯度下降的"步子"；太大会震荡发散，太小爬得慢 |
| `lr_schedule: cosine` | 训练后期把步长平滑缩到近零 | 收敛前的指数退火/收尾减速 |
| `epochs` / `patience` | 最多学几遍 / 连续多少遍没进步就早停 | 最大重试次数 / 熔断阈值 |
| `loss` | 判"错得多惨"的规则（mse/huber/l1） | 打分细则；细则改了，模型优化的目标就变了 |
| `event_weight` | 对"在烧水"的样本把惩罚乘倍（λ 加权） | 给稀疏错误加权 |
| `batch_size` | 每次看多少题再更新一次 | 攒一批再提交 |
| `weight_decay` | 惩罚过大的参数（轻防过拟合） | 代码Lint的硬约束 |
| `window_size` | 看多久历史（128 点≈12.8 分钟） | 日志窗口 |
| `max_samples_train/val/test` | 从各段抽多少个窗 | 采样率；**改了它=换了考卷**（见 §5 铁律 1） |
| `event_boost` | 每个 epoch 按概率把部分样本换成含烧水事件的窗 | 提高难例抽检频率 |
| `select_on` | 用 val 的哪个指标决定"留哪版权重" | 用哪个 KPI 做晋级评审 |
| `weight_avg: k` | 把 val 前 k 名的权重做平均，替代单点冠军 | 多份配置取中位数发布 |
| `seed` | 随机数种子（初始化/打乱顺序） | 同 seed=同一次随机抽签剧本，可复现 |

**过拟合（overfitting）**：train 分很高、val 分平平——背题不会举一反三。**欠拟合**：train 分都不高——容量或训练量不够。判断依据就是 train/val 的差距方向。

## 4. 调参的核心方法论：把训练当"分布式系统调优"做

本项目定下的五条铁律，每条都有对应的踩坑史（§5–§7 会看到实例）：

1. **固定评测口径（三口径各司其职，对照结果见 `reports/tuning_rounds.csv` 的 `protocol_diff` 列）。** 三个采样键各有各的「不能动」的理由：
   - `max_samples_test` = **考卷**：`build_splits` 对窗中心做 `linspace(首,末,N)`，N 一变整套考题就换（6k 上的 12.46W 和 30k 上的 11.67W 是两个世界）。跨轮直接比数值必须同 N；不同 N 先 `eval_ckpt.py --max-samples-test 30000` 统一复评。
   - `max_samples_val` = **晋级评审的答卷**：留哪版权重由 val 成绩决定（选 best epoch / top-k 权重平均都读它）。val 样本一变，「同一配置」也可能选留不同权重——单变量对比必须冻结它；要研究「选点标尺本身」（如 A1 把 val 从 8k 加密到 30k）则把它当显式实验变量并标注为协议变更。
   - `max_samples_train` = **学习材料**：可以动，但它本身就是「数据规模」这一独立因子——改它再叠别的改动=双变量实验（历史教训：8k 上被否的 d128 在全量翻案）。注意两个隐性耦合：事件增广替换量 n×event_frac 随它等比缩放（掺入强度被隐性改掉）、每 epoch 更新步数=n/batch 也随之变。安全项：归一化统计按 train 全段计算、与抽样无关（`src/data.py`）。
2. **一轮只动一个变量。** 多因子同时改 = 出结果无法归因（F5 三个一起上，好坏说不清，白跑）。
3. **val 定生死，test 只见一次。** 选择用 val；test 只在配置冻结后跑一次作验收。
4. **改动要过"噪声地板"才算数。** 本项目实测：同配置换 seed，MAE 波动 ±0.5W、检测指标波动 ±1.9pp（n_ON=266 时的统计噪声 1σ=1.84pp）。**小于地板的提升一律视为噪声，不得写进结论**。
5. **失败也是数据。** 13 条死路全部落盘（CSV 里 status=否决/判死），后来者才不会重走。

## 5. 战史第一课：锚点与单因子排查（Phase 0–1）

### 5.0 先立锚点
- **缩短版 baseline**（10k 样本×10 轮，CPU 1 小时能跑完）：MAE 13.62W，P .933 / R .712。作用=确认代码-数据-指标链路通、给后续一切改动一个参照物。**任何调优项目第一步都是它。**
- **稠密复评**：baseline 的权重在 30k 稠密考卷上重考一次 → 11.67W。此后所有轮次都对齐这份考卷。
  工程含义：先有基准测试（benchmark），再谈优化。

### 5.1 六个单因子实验一次并行铺开（8k 小口径×12ep）

针对锚点的症状（误差大、R 低、总电量低估 21%），一次提六个**可证伪的小假设**，每个只动一个键：

| 实验 | 假设（怀疑什么病） | 结果 | 判定 |
|---|---|---|---|
| loss→l1 | 猜错 2000W 和 20W 罚得不一样重，是不是委屈了小信号？ | **R²=−0.009，全线崩溃** | ☠️ L1 求"中位数"，99% 零样本下中位数=0 → 模型学会全猜 0。打分细则和分布不匹配时，换个 loss 能直接学废 |
| window 128→256 | 上下文太短，看不全一次完整烧水？ | 本沙箱内存线性暴涨被 OOM 杀 | ⏸ 没测成，教训入库（CPU 环境显式内存预算先行） |
| d64→128 | 脑容量不够（欠拟合）？ | 小数据下更差（val 5.39 劣于 d64 同规模基线） | ✗ 被否——但**后来在全量+正确正则下翻案成功**（小样本时的否决≠永久否决，外推要复核，见 §7） |
| lr→1e-3 | 12 轮内步子太小学不完？ | MAE 略好但 R²/电量偏差恶化 | ✗ "跑得快"不等于"学得好" |
| layers 2→4 | 网络太浅表达不了？ | 无收益 | ✗ |
| **dropout 0.1→0** | **答案本身有错，dropout 在教模型"没把握就报 0"** | 8k 口径 MAE 6.65（六泳道最佳）、R .712→.900；10k A/B 复测 −7.7% | ✅ 六连里唯一采纳 |

**为什么 dropout=0 有效（这是本项目最重要的一个洞见）**：烧水壶的真值标签来自子电表，而子电表经常**丢数据**——壶明明在烧、标签却是 0（约占事件窗的三成）。dropout 的原理是"随机打瞎一部分输入，逼模型只信确定的模式"；但当"确定模式"本身被标注噪声污染时，这个机制会系统性教模型**遇到可疑就报 0** → 召回被压制。把正则关掉，模型敢报事件了，漏报立刻减半。
**给工程师的类比**：正则化=对不确定的输入降权。如果你的"不确定"其实来自**日志丢失**而不是模型没把握，降权就是在惩罚正确答案——**先修数据，再谈正则**。

### 5.2 组合实验翻车（Phase 2）
两个各自有效的因子（drop0 + lr1e-3）拼起来反而 +30.5%；再赌一把更大 lr 直接发散。
**结论**：超参不是可加性配置，A 有效 + B 有效 ⇏ A∧B 有效。组合必须实测。

### 5.3 小→大复核（Phase 3，防"只在测试环境好使"）
把 Phase1/2 的冠军拿到"和 baseline 完全同口径"的 10k/6k 尺度重测：8k 上 6.65 的成绩到 10k 只剩 12.57——**小口径排名不能直接外推**，重大采纳前必须在接近最终口径上复核。同时用"只动 lr 的 A/B 对"证明真正有效的是 drop0。

## 6. 战史第二课：全量定型与"选择器也是超参"（Phase 4）

- **F2**（drop0 全量 30k、20ep、固定 lr）：10.59W——数据量 ×3 收益远低于预期。看训练曲线：尾段 val 在平台上来回震荡 → **早停在 4.93↔11.67 的震荡里选到了次优 epoch**（选点=掷骰子）。
- **F3**（只加一行 `lr_schedule: cosine`）：**8.58W，R² .723**。原理：后期步长平滑趋零，曲线不再抖，早停/选点都变稳。这一改动贡献 −19%。
- **教训 5**（REPORT.md §3.5 的展开）：**"用哪个指标、在哪个 epoch 留下权重"（模型选择）本身是个高影响超参**，很多团队只调模型参数不调它。本项目后来给它做了可选项 `select_on: f1|minprf` 和 `weight_avg:k`（前 k 名权重取平均，相当于"发布配置的中位数"而非"单点冠军"）——F10 吃到红利。

## 7. 战史第三课：攻坚"双 0.9"与标签噪声天花板（Phase 5）

目标：P>0.9 且 R>0.9（当时另有 MAE≤6W 约束）。从 F3（P .907/R .805）出发连开十条泳道，**全部基于同一个尸检**：F10@500W 口径下漏报 54 个 ≫ 误报 14 个，且总电量系统性低估 ~10–20% → 模型"太胆小"。于是所有实验都在赌"怎么让它敢报火"：

| 泳道 | 思路（人话） | 结果 vs F3=8.58W |
|---|---|---|
| F4 λ8 | 把"在烧水"样本的扣分×8 | MAE +60%（电量偏差倒是修好了）→ 方向对剂量错 |
| F4' λ3+f1选点 | 温和×3 + 换晋级评委 | +12%，三因混杂说不清谁有功 |
| F6 事件过采样 | 训练集里多掺几倍烧水样本 | +8.5%；尸检发现各模型**漏的是同一批事件**（重合 98%）→"见少了"假设破产 |
| F7 每轮重抽 45% | 不改变样本总量，每个 epoch 换一批掺入的烧水窗 + 权重取平均 | +4.6%，但 **P .951 历史新高**——保守换精确的老毛病第一次松开 |
| F9 λ2+min(P,R) 选点 | 把"短板指标"直接写进晋级标准 | +37.9%，☠️ 噪声标签下"专挑短板"会挑出一个畸形儿 |
| F8 d128/4L 扩容 | 平台期了 → 加容量 | 截停快照已 −18%：容量是真瓶颈，当年 8k 的否决是"小数据×dropout"的混杂伪像 |
| **F10 = F8 配方跑满 30ep + 权重平均** | 补全 F8 没做完的部分 | **6.68W / R² .770 / EE −10%**。R .797 仍差最后 1 步 |
| F12/F13 | 数据抖动增强 / 换 seed | 各 +8~14%；**换 seed 就差 ±0.5W** → 定下噪声地板 |
| median4 集成 | 让 f8/f10/f12/f13 四个模型**投票取中位数** | 6.99W；P .907 / R .880，最接近双线 |

**为什么"λ 加权/过采样/换评委"全军覆没，而扩容+平均活下来**：前一类都在"放大少数类的声音"，但少数类的标签里有三成是**噪声**（见 §5.1）——放大信号=连同错误监督一起放大，模型要么更保守要么直接学偏。真正有效的两件事都不碰标签分布：把模型做大（学得动真模式）、把权重平均（少踩随机坑）。
**类比**：错误告警太多时，正确操作不是给"告警样本"加权（日志本身缺了 30%），而是升级检测器（容量）+ 多班倒复核（权重平均/集成）。

**"双 0.9 被锁定"的定量证据链**（为什么判定不可达，而不是"再调调就行"）：
1. 稠密 test 上真烧水窗只有 **266 个**（tiny sample！）——分数天然带 ±1.8pp 的统计噪声（1σ），改配置看到 +1pp 提升基本等于没改；
2. 各模型的**漏报重合 98%**：不是哪个模型不行，是那 ~29% 的漏报事件在总表曲线上根本看不见（子电表没数据、总表 bump 极小），物理不可学；
3. 误报里 ~3/4 是**标签错**（壶真烧了、标签是 0）——把这部分扣掉，P 立刻 .906→.976 过线。
→ 结论：**在"点标签 + 500W 双侧同阈值 + 含噪数据"的口径内，调参已到天花板**；想达标必须改口径（运营阈值/剔噪/事件级评测），这就是下一阶段 A1 的由来。

## 8. 战史第四课：阈值、集成与"考卷也要调"（A1，2026-09-07）

- **集成取中位数而不是平均**：四个模型对同一窗各报一个功率，取 median——一个成员犯疯话（比如过采样练出来的 F12 偶尔爆高），平均会带偏整体，中位数免疫。这跟"多副本取中位延迟"是同一个思想。
- **决策阈值与标签阈值解耦 = "运营点"**：评测口径要求"预测>500W 才算报火"，但**生产部署里你说了算的报警线不必等于 500**。把预测端阈值降到 ~95–115W 的扫描实验显示跷跷板整体被抬高：median4 在 t=95 时 P/R 同时 .906。
- **A1 实验修的是"选点用的考卷太小"**：旧版在 val8k（只有 ~80 个烧水窗）上选阈值，选到 t*=115 → 考试 P .907/R .880——**与最优值只差"选点误差"0.026**。把 val 也加密到 30k（173 个烧水窗）再选，**独立选中 t*=95，test 三指标齐 .9060、SAE .189，四元约束点估计全过**。
  人话：**不是你调得不好，是调参用的监控采样率不够**。staging 环境指标粒度太粗时，先把 staging 的样本量堆上去，再谈"哪个配置更好"。
- **两个免费副产品**：① 时序滤波（邻居窗互保才报警）被点标签机制**证伪**——真事件在预测序列上本来就是孤立单点，滤波会连真带假一起杀；说明任何后处理必须配事件级口径。② bootstrap 置信区间给出 min(P,R)∈[.868,.940]，"点估计达标"和"统计达标"之间还差 2 个补员种子（B3，每条 ~2h CPU）。

## 9. 可复制的调参 SOP（把本项目抽成清单）

1. **锚点**：先跑一个 1 小时内能完赛的最小配置，落盘指标；同时把"公平考卷"定死（固定 max_samples_test，跨口径一律 eval_ckpt 复评）。
2. **体检**：拆指标找病因——误差集中在大值还是小值？P/R 谁偏科（看漏报/误报计数，不看比率）？电量偏差符号=系统性胆大胆小？训练/val 曲线形态=过/欠拟合？**每个病灶写成一个可证伪假设**。
3. **单因子矩阵**：一批平行小实验，每个只动一个键；跑完按"是否过噪声地板"裁决（地板怎么定？同配置换 seed 跑两次，差值=地板）。
4. **组合与复核**：胜出因子才许组合（组合需实测，不假定可加）；采纳前在接近最终口径上复测。
5. **全量定版**：只带"复核过的收益"上全量；此后 test 只考一次。
6. **卡住时按序开大炮**：曲线平台→加容量；选点抖动→lr 调度/权重平均；评测噪声大→**先加密 val/test**；再卡→集成；标签可疑→做噪声地板尸检（抽查 FN/FP 原始波形），必要时修数据而不是修模型。
7. **收尾**：死路也进台账；写清"结论在什么口径下成立"；给出复现命令。

## 10. 自己动手跑（命令级）

```bash
# 环境（Linux 沙箱，无 conda）
python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt

# A) 复现稳定版训练（CPU ~2h；有 GPU 自动用 GPU）
./.venv/bin/python scripts/train.py --config reports/tune_f10_biglong/config.yaml \
    --data-path data/ukdale_prepared.npz --out reports/my_run

# B) 任何旧 checkpoint 换到稠密考卷复评（跨 run 公平对比的标准姿势）
./.venv/bin/python scripts/eval_ckpt.py --config reports/<run>/config.yaml \
    --ckpt reports/<run>/best.pt --data-path data/ukdale_prepared.npz \
    --max-samples-test 30000

# C) 阈值扫描（看 P/R 跷跷板曲线，定运营点）
./.venv/bin/python scripts/threshold_scan.py --help

# D) 重建全调参史数据表（每轮参数/指标/为什么调/与父轮差值）
./.venv/bin/python scripts/build_tuning_csv.py        # → reports/tuning_rounds.csv
./.venv/bin/python scripts/a1_val30k.py                # A1 复扫（幂等，有预测缓存）
```

## 11. 本项目踩过的坑（工程师版排雷清单）

1. **口径不一致就对比**——比出个寂寞（本项目曾用三种 test 规模，后全部 eval_ckpt 对齐；CSV 里跨协议行的差值列干脆留空）。
2. **小结果外推**——d128 在 8k 被否、在全量翻案成最大增益；任何"否"都有适用范围，值得记"在 X 规模下否决"。
3. **选择器噪声**——早停/选 best epoch/选阈值用的指标如果样本少，等于掷骰子（F2 踩过、A1 修过）。
4. **正则化惩罚了正确的答案**——标签有噪声时 dropout/加权 loss 都可能是反向优化（§5.1/§7）。
5. **统计不显著当收益**——先测噪声地板（换 seed），差值不过线的结论不进报告。
6. **OOM/超时工程项**：CPU 沙箱单泳道先行测 RSS；训练 timeout 按 pace×1.5；改 yaml 用 python+yaml 库，sed 静默失配曾把 F11 变成"意外复现实验"（好消息：证明了管道确定性）。
7. **test 偷看**——用 test 挑配置/挑阈值，线上必翻车；本项目连"达标待复验"都按预注册规则记档。

## 12. 术语速查表

| 术语 | 一行解释 |
|---|---|
| epoch / batch | 把训练集过一遍 / 每次攒 64 个样本更新一次参数 |
| learning rate | 每次更新的步幅；配 cosine = 后期自动减速 |
| 早停 (patience) | 连续 N 轮 val 不进步就停，留 best |
| checkpoint / best.pt | 某一版权重快照 / val 最优那版 |
| 过拟合 / 欠拟合 | 背题 / 没学会 |
| 正则化 (dropout, weight decay) | 防背题的各种"罚" |
| 标签噪声 | 标准答案本身错了/丢了 |
| 召回/查准 | 漏报率补集 / 误报率补集 |
| 阈值校准 / 运营点 | 改"报不报"的判定线（≠评测定义线） |
| 集成 (median4) | 多模型投票取中位 |
| 权重平均 (SWA/wavg) | 多版权重取平均，代替单点冠军 |
| 消融 (ablation) | 逐个拆掉部件验证每个部件的贡献 |
| 噪声地板 | 什么都不改、只换随机种子得到的分数波动范围 |
| 1σ / 置信区间 | 采样噪声的刻度；266 个正样本 → ±1.8pp，谁先别得意 |
| bootstrap | 对评测集重采样几千次，直接量出分数的不确定性 |
| 幂等脚本 | 同一命令重复跑结果一致，本项目所有分析都是脚本不是手算 |

## 13. 各文档都有什么（阅读顺序）

1. 本指南 —— 怎么想、为什么这么做（先读我）
2. `README.md` —— 装环境、跑训练、目录结构
3. `REPORT.md` —— 只放验证过的稳定结论 + 当前推荐版本与复现命令
4. `reports/tuning_rounds.csv` —— 26 轮全量账本：每轮改了什么参数、指标前后差值、**为什么这么改**（rationale 列）
5. `REPORT_TEST.md` —— 每轮专题的全过程（假设/方法/结果/教训），想深挖某一步的原始论证看它
6. `STATUS.md` + `session/` —— 项目当前状态与会话流水（工程交接用）
7. 代码地图：`src/{data,model,trainer,metrics,experiment,device}.py`（全部调参开关的实现在 trainer/data/experiment，均 opt-in、默认关闭不影响旧口径）；`scripts/`（train/eval_ckpt/threshold_scan/build_tuning_csv/a1_val30k）
